# Snippet for ~/.berzcoin/berzcoin.conf (INI — merge into your [main] section)
#
# Static peers (always eligible for outbound connections)
# addnode = 10.0.0.1:8333,192.168.1.5:8333
#
# Connect-only: use ONLY these peers (disables DNS seed usage for outbound discovery)
# connect = 10.0.0.1:8333
#
# Optional JSON file under datadir listing bootstrap_nodes (see configs/bootstrap_nodes.json)
# bootstrap_file = bootstrap_nodes.json
# bootstrap_enabled = true
