# CI workflow copies

These files mirror [`.github/workflows/`](../.github/workflows/). GitHub Actions loads workflows only from `.github/workflows/`; copies here stay in sync for reference or non-GitHub runners.
